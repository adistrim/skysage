{
    "version": 2,
    "builds": [
        {
            "src": "Dockerfile"
        }
    ],
    "routes": [
        {
            "src": "/static/(.*)",
            "dest": "/static/$1"
        },
        {
            "src": "/(.*)",
            "dest": "/main.py"
        }
    ]
}
